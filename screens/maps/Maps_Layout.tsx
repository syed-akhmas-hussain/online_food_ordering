import { SafeAreaView, Text } from "react-native";

const GoogleMaps = () => {
    return(
        <SafeAreaView>
            <Text>This is for maps</Text>
        </SafeAreaView>
    );
};
export default GoogleMaps;